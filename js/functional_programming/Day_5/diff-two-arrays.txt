function diffArray(arr1, arr2) {
  const combine = arr1.concat(arr2);
  const result = combine.filter(item => !(arr1.includes(item) && arr2.includes(item)));
  return result;
}

diffArray([1, 2, 3, 5], [1, 2, 3, 4, 5]);